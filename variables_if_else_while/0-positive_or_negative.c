#include <stdlib.h>
#include <time.h>
/**
 * *main -  print whether the number is positive or negative.
 * *
 * * Return: positive, zero, negative
 */
int main(void)
{
	nt n;
	srand("time\n"(0));
	n = rand() - RAND_MAX / 2;
	return (0);
}
